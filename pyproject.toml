[project]
name = "epic-performance-benchmark"
version = "0.1.0"
description = "A simple performance benchmark for various languages."
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package-mode = false
